plugins {
    id 'com.android.application'
}
apply from: '../common.gradle'

android {
    namespace 'com.ff.mvvm'
    defaultConfig {
        applicationId "com.ff.mvvm"
    }
    // 添加DataBinding依赖
    buildFeatures {
        dataBinding = true
    }
}